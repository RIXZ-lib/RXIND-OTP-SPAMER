 # RXIND OTP Spammer

RXIND OTP Spammer is a Python-based tool for education and security testing of authentication systems. This tool sends OTP requests.

⚠️ **Disclaimer:** This tool is created for educational and ethical hacking purposes. Its use for spam, harassment, or other illegal activities is the sole responsibility of the user; the creator is not liable for any misuse. Use only on systems you own or have explicit permission to access.

## Installation in Termux

```bash
termux-setup-storage
pkg update && pkg upgrade -y
pkg install python git -y
git clone https://github.com/RIXZ-lib/RXIND-OTP-SPAMER.git
cd RXIND-OTP-SPAMER
pip install -r requirements.txt
python main.py
```

## Installation in Windows

```cmd
git clone https://github.com/RIXZ-lib/RXIND-OTP-SPAMER.git
cd RXIND-OTP-SPAMER
pip install -r requirements.txt
python main.py
```

## Installation in Linux (Ubuntu/Debian/Kali)

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install python3 python3-pip git -y
git clone https://github.com/RIXZ-lib/RXIND-OTP-SPAMER.git
cd RXIND-OTP-SPAMER
pip3 install -r requirements.txt
python3 main.py
```

## Installation in macOS

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install python git
git clone https://github.com/RIXZ-lib/RXIND-OTP-SPAMER.git
cd RXIND-OTP-SPAMER
pip3 install -r requirements.txt
python3 main.py
```

## Installation in Replit (Online)

1. Create Repl → Import from GitHub
2. Enter: `https://github.com/RIXZ-lib/RXIND-OTP-SPAMER.git`
3. Click Import
4. Open Shell tab
```bash
pip install -r requirements.txt
python main.py
```

---
**tools free**  
**Thanks for using RXIND!**  
